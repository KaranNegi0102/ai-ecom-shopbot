import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "shorturl.at",
      "images.unsplash.com",
      "plus.unsplash.com",
      "images.pexels.com",
      "tinyurl.com",
    ],
  },
};

export default nextConfig;
